require "spec_helper"

RSpec.describe Charyf::Memory::Storage do
  it "has a version number" do
    expect(Charyf::Memory::Storage::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
